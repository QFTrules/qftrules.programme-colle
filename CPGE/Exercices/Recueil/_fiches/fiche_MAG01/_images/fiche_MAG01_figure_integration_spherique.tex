\newenvironment{3dtikzpicture}[3][]{
    \pgfmathsetmacro\xx{cos(#3)}
    \pgfmathsetmacro\xy{cos(#2)*sin(#3)}
    \pgfmathsetmacro\yx{-sin(#3)}
    \pgfmathsetmacro\yy{cos(#2)*cos(#3)}
    \pgfmathsetmacro\zy{sin(#2)}
    \begin{tikzpicture}[x={(\xx cm,\xy cm)},y={(\yx cm,\yy cm)},z={(0,\zy cm)},#1]
    }{
    \end{tikzpicture}
}

\resizebox{1\textwidth}{!}{

\begin{3dtikzpicture}{75}{260}
    
    \def\rayon{4.5};

    \shade[ball color = lightgray, opacity = 0.5] (0,0,0) circle (4.5cm);
    %\filldraw[ball color = lightgray, opacity = 0.5] plot[domain=0:2*pi](0{4.5*cos(\x r)}, {4.5*sin(\x r)});

    % draw arcs
    \draw[dashed,gray] plot[domain=0:2*pi]({4.5*cos(\x r)}, {4.5*sin(\x r)}, 0);

    \draw[dashed,gray] (0,0,0) -- ({4.5*sin(60)},{-4.5*cos(60)},0) node[midway,above]{$R$};


    \draw(0,0,0) node[above left]{$O$};
    
    \draw[-{>[scale=2]}] (0,0,0)--(0,5.5,0); %axe y
    \draw (0,5.5,0) node[below]{$y$};
    
    \draw[-{>[scale=2]}] (0,0,0)--(0,0,5.5); %axe z
    \draw (0,0,5.5) node[above]{$z$};
    
    \draw[-{>[scale=2]}] (0,0,0)--(5.5,0,0); %axe x
    \draw (5.5,0,0) node[below]{$x$};
    
    %vecteur r
    \draw[red] (0,0,0) -- (2,2,3);
    \draw (1,1,1.5) node[above]{$r$};
    %\draw[red, dashed] (0,0,0) -- (2,2,0);
    
    %Coordonnée du point M premier point du cube
    %Face supérieure A -- B -- C -- D -- A;
    %Face inférieure E -- F -- G -- H -- E;
    \def\xA{4.12};
    \def\yA{0};
    \def\zA{0};
    
    %Angle phi en degré
    \def\phi{44.4};
    %Angle beta en degré
    \def\beta{-90+43.3};
    
    %A
    \path let \n1 = { \xA*cos(\beta)*cos(\phi)+\zA*sin(\beta)*cos(\phi)-\yA*sin(\phi) },
    \n2 = { \xA*cos(\beta)*sin(\phi)+\zA*sin(\beta)*sin(\phi)+\yA*cos(\phi) },
    \n3 = { -\xA*sin(\beta)+\zA*cos(\beta) }
    in coordinate (A) at (\n1, \n2, \n3);
    \draw (A) node{$\bullet$};
    
    %B
    \path let \n0 = {\xA+1}, \n1 = { \n0*cos(\beta)*cos(\phi)+\zA*sin(\beta)*cos(\phi)-\yA*sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\zA*sin(\beta)*sin(\phi)+\yA*cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\zA*cos(\beta) }
    in coordinate (B) at (\n1, \n2, \n3);
    %\draw (B) node{$\bullet$};
    %\draw (B) node[above left]{B};
    
    %C
    \path let \n0 = {\xA+1}, \n4 = {\yA+1}, \n1 = { \n0*cos(\beta)*cos(\phi)+\zA*sin(\beta)*cos(\phi)-\n4*sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\zA*sin(\beta)*sin(\phi)+\n4*cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\zA*cos(\beta) }
    in coordinate (C) at (\n1, \n2, \n3);
    %\draw (C) node{$\bullet$};
    %\draw (C) node[above left]{C};
    
    %D
    \path let \n0 = {\yA+1}, \n1 = { \xA*cos(\beta)*cos(\phi)+\zA*sin(\beta)*cos(\phi)-\n0*sin(\phi) },
    \n2 = { \xA*cos(\beta)*sin(\phi)+\zA*sin(\beta)*sin(\phi)+\n0*cos(\phi) },
    \n3 = { -\xA*sin(\beta)+\zA*cos(\beta) }
    in coordinate (D) at (\n1, \n2, \n3);
    %\draw (D) node{$\bullet$};
    %\draw (D) node[above left]{D};
    
    %E
    \path let \n0 = {\zA-1}, \n1 = { \xA*cos(\beta)*cos(\phi)+\n0*sin(\beta)*cos(\phi)-\yA*sin(\phi) },
    \n2 = { \xA*cos(\beta)*sin(\phi)+\n0*sin(\beta)*sin(\phi)+\yA*cos(\phi) },
    \n3 = { -\xA*sin(\beta)+\n0*cos(\beta) }
    in coordinate (E) at (\n1, \n2, \n3);
    %\draw (E) node{$\bullet$};
    %\draw (E) node[above left]{E};
    
    %H
    \path let \n0 = {\zA-1}, \n4 ={\yA+1}, \n1 = { \xA*cos(\beta)*cos(\phi)+\n0*sin(\beta)*cos(\phi)-\n4*sin(\phi) },
    \n2 = { \xA*cos(\beta)*sin(\phi)+\n0*sin(\beta)*sin(\phi)+\n4*cos(\phi) },
    \n3 = { -\xA*sin(\beta)+\n0*cos(\beta) }
    in coordinate (H) at (\n1, \n2, \n3);
    %\draw (H) node{$\bullet$};
    %\draw (H) node[above left]{H};
    
    %G
    \path let \n0 = {\zA-1}, \n4 ={\yA+1}, \n5 ={\xA+1}, \n1 = { \n5 *cos(\beta)*cos(\phi)+\n0*sin(\beta)*cos(\phi)-\n4*sin(\phi) },
    \n2 = { \n5 *cos(\beta)*sin(\phi)+\n0*sin(\beta)*sin(\phi)+\n4*cos(\phi) },
    \n3 = { -\n5 *sin(\beta)+\n0*cos(\beta) }
    in coordinate (G) at (\n1, \n2, \n3);
    %\draw (G) node{$\bullet$};
    %\draw (G) node[above left]{G};
    
    %F
    \path let \n0 = {\zA-1}, \n5 ={\xA+1}, \n1 = { \n5 *cos(\beta)*cos(\phi)+\n0*sin(\beta)*cos(\phi)-\yA*sin(\phi) },
    \n2 = { \n5 *cos(\beta)*sin(\phi)+\n0*sin(\beta)*sin(\phi)+\yA*cos(\phi) },
    \n3 = { -\n5 *sin(\beta)+\n0*cos(\beta) }
    in coordinate (F) at (\n1, \n2, \n3);
    %\draw (F) node{$\bullet$};
    %\draw (F) node[above left]{F};
    
    %Arrêtes du cube
    \draw[blue](A)--(B)--(C);
    \draw[blue, dashed](C)--(D)--(A);
    \draw[blue](E)--(F)--(G);
    \draw[blue] (G)--(H)--(E);
    \draw[blue](A)--(E);
    \draw[blue](B)--(F);
    \draw[blue](C)--(G);
    \draw[blue, dashed](D)--(H);
    
    %\text{d} \tau
    \draw[blue] (4.5, 7, 3) node[above]{$\text{d} \tau = r^2\sin \theta \, \text{d} r \, \text{d} \theta \, \text{d} \varphi $};
    
    
    
    %Projection
    \draw[red, dashed] (0,0,0) -- (E);
    
    %A_z0
    \path let \n1 = { \xA*cos(\beta)*cos(\phi)+\zA*sin(\beta)*cos(\phi)-\yA*sin(\phi) },
    \n2 = { \xA*cos(\beta)*sin(\phi)+\zA*sin(\beta)*sin(\phi)+\yA*cos(\phi) },
    \n3 = { -\xA*sin(\beta)+\zA*cos(\beta) }
    in coordinate (A0) at (\n1, \n2, 0);
    \draw[red, dashed] (A) -- (A0);
    \draw[red, dashed] (0,0,0) -- (A0);
    
    %D_z0
    \path let \n0 = {\yA+1}, \n1 = { \xA*cos(\beta)*cos(\phi)+\zA*sin(\beta)*cos(\phi)-\n0*sin(\phi) },
    \n2 = { \xA*cos(\beta)*sin(\phi)+\zA*sin(\beta)*sin(\phi)+\n0*cos(\phi) },
    \n3 = { -\xA*sin(\beta)+\zA*cos(\beta) }
    in coordinate (D0) at (\n1, \n2, 0);
    
    \draw[red, dashed] (D) -- (D0);
    \draw[red, dashed] (0,0,0) -- (D0);
    \draw ($0.5*(D0)+(0.6,-0.3,0) $) node[below] {$r\sin\theta$};
    \path (0.95,1.2,0)++(80:0) node[right] {$\text{d} \varphi$};
    
    \draw[blue, ->] (A0) -- (D0);
    \draw ($0.5*(A0) + 0.5*(D0)+(-0.3,0.1,0) $) node[below right] {$r\sin\theta \text{d} \varphi$};
    
    \draw [->] (1,1,0) arc (45:73:1);
    
    \draw[->] ($0.5*(A)$) to[out=-10,in=90] ($0.5*(E)$);
    \draw ($0.5*(A)+(0,0.2,0)$) node[right] {$\text{d} \theta$};
    
    
    %Coordonnées flèche \text{d} r
    \path let \n0 = {\xA}, \n4 = {\yA}, \n5 = {\zA + 1}, \n1 = { \n0*cos(\beta)*cos(\phi)+\n5*sin(\beta)*cos(\phi)-\n4 *sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\n5*sin(\beta)*sin(\phi)+\n4 *cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\n5*cos(\beta) }
    in coordinate (ddr1) at (\n1, \n2, \n3);
    
    \path let \n0 = {\xA+1}, \n4 = {\yA}, \n5 = {\zA + 1}, \n1 = { \n0*cos(\beta)*cos(\phi)+\n5*sin(\beta)*cos(\phi)-\n4 *sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\n5*sin(\beta)*sin(\phi)+\n4 *cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\n5*cos(\beta) }
    in coordinate (ddr2) at (\n1, \n2, \n3);
    
    \path let \n0 = {\xA+0.5}, \n4 = {\yA}, \n5 = {\zA + 1.3}, \n1 = { \n0*cos(\beta)*cos(\phi)+\n5*sin(\beta)*cos(\phi)-\n4 *sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\n5*sin(\beta)*sin(\phi)+\n4 *cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\n5*cos(\beta) }
    in coordinate (ddr3) at (\n1, \n2, \n3);
    
    \draw [dashed, red] (ddr1) -- (A);
    \draw [dashed, red] (ddr2) -- (B);
    \draw [blue, ->] (ddr1)--(ddr2);
    \draw (ddr3) node{$\text{d} r$};
    
    
    %Coordonnées flèche r \text{d} \theta
    \path let \n0 = {\xA+3.5}, \n4 = {\yA+1}, \n5 = {\zA}, \n1 = { \n0*cos(\beta)*cos(\phi)+\n5*sin(\beta)*cos(\phi)-\n4 *sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\n5*sin(\beta)*sin(\phi)+\n4 *cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\n5*cos(\beta) }
    in coordinate (ddtheta1) at (\n1, \n2, \n3);
    
    \path let \n0 = {\xA+3.5}, \n4 = {\yA+1}, \n5 = {\zA - 1}, \n1 = { \n0*cos(\beta)*cos(\phi)+\n5*sin(\beta)*cos(\phi)-\n4 *sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\n5*sin(\beta)*sin(\phi)+\n4 *cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\n5*cos(\beta) }
    in coordinate (ddtheta2) at (\n1, \n2, \n3);
    
    \path let \n0 = {\xA+4}, \n4 = {\yA+1}, \n5 = {\zA - 0.5}, \n1 = { \n0*cos(\beta)*cos(\phi)+\n5*sin(\beta)*cos(\phi)-\n4 *sin(\phi) },
    \n2 = { \n0*cos(\beta)*sin(\phi)+\n5*sin(\beta)*sin(\phi)+\n4 *cos(\phi) },
    \n3 = { -\n0*sin(\beta)+\n5*cos(\beta) }
    in coordinate (ddtheta3) at (\n1, \n2, \n3);
    
    \draw [dashed, red] (ddtheta1) -- (C);
    \draw [dashed, red] (ddtheta2) -- (G);
    \draw [blue, ->] (ddtheta1)--(ddtheta2);
    \draw (ddtheta3) node{$r \text{d} \theta$};
    
    
    
    \draw (2,2,3) node[above left]{$\ptM$};
    
\end{3dtikzpicture}

}