\input{tikz.tex}
% \documentclass{article}
% \usepackage{amsmath}
% \usepackage[dvipsnames]{xcolor}
% \pagestyle{empty}
% \usepackage{miller}
% \usepackage{tikz}
% \usepackage{tikz-3dplot}
% \usetikzlibrary{calc}
% \pgfdeclareradialshading{sphere4}{\pgfpoint{-0.2cm}{0.5cm}}% 
% {rgb(0cm)=(1,1,1);
% rgb(0.2cm)=(0.7,0.1,0); rgb(0.9cm)=(0.5,0.05,0); rgb(1.05cm)=(1,1,1)}

\begin{document}

% set view angle
% \tdplotsetmaincoords{74}{113}% rot x rot z

\begin{tikzpicture}[scale=1.5]

\def \s {0}     % defines shear of z along y (instead of angles), for orthogonal systems: 0
\def \su {0}   % defines shear of x along y (instead of angles), for orthogonal systems: 0
\def \a {1}       % unit cells along a (only integers!)
\def \b {1}       % unit cells along b (only integers!)
\def \c {1}       % unit cells along c (only integers!)

% lattice directions parallel to c
 \foreach \u in {0,1,...,\a}
    \foreach \v in {0,1,...,\b}
      \foreach \w in {0,1,...,\c}
        \draw[thin,gray] (\u+\su*\v,\v,0) -- (\u+\su*\v,\v+\s*\w,\w); 
% lattice directions parallel to b
 \foreach \u in {0,1,...,\a}
    \foreach \v in {0,1,...,\b}
      \foreach \w in {0,1,...,\c}
        \draw[thin,gray] (\u,0+\s*\w,\w) -- (\u+\su*\v,\v+\s*\w,\w); 
% lattice directions parallel to a
 \foreach \u in {0,1,...,\a}
    \foreach \v in {0,1,...,\b}
      \foreach \w in {0,1,...,\c}
        \draw[thin, gray] (0+\su*\v,\v+\s*\w,\w) -- (\u+\su*\v,\v+\s*\w,\w); 

\node[] at (-0.5,0.25,0) {$a$};

% plot atom at cube center
\draw[thin,gray] (0,\b,\c) -- (\a/2,\b/2,\c/2) node[pos=0.4,below,black] {$r$};
\shade[ball color=JoliBleu] (\a/2,\b/2,\c/2) circle(0.1) ;
% basis vectors
% \draw[axis] (0,0,0) -- (1,0,0) node[left] {$\bf a$};
% \draw[axis] (0,0,0) -- (\su,1,0) node[right] {$\bf b$};
% \draw[axis] (0,0,0) -- (0,\s,1) node[above] {$\bf c$};
\foreach \u in {0,1,...,\a}
   \foreach \v in {0,1,...,\b}
     \foreach \w in {0,1,...,\c}
       \shade[ball color=JoliRouge] (\u+\su*\v,\v+\s*\w,\w) circle(0.1);

% lattice vectors
% \draw[thick,red] (0,0,0) -- (0+\su*3,3+\s*1,1) node[right] {\hkl[031]};
% \draw[thick,red] (0,0,0) -- (1+\su*3,3+\s*2,2) node[right] {\hkl[132]};
% \draw[thick,red] (0,0,0) -- (2+\su*1,1+\s*2,2) node[right] {\hkl[212]};
% \draw[thick,red] (0,0,0) -- (3+\su*3,3+\s*0,0) node[right] {\hkl[110]};
% \draw[thick,red] (0,0,0) -- (3+\su*1,1+\s*0,0) node[right] {\hkl[310]};


\end{tikzpicture}

\end{document}