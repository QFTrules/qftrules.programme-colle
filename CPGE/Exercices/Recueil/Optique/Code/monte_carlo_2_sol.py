plt.hist(fp,                        # histogramme
         bins = 40)                 # nombre de boîtes
plt.title(r"Distribution de f'")    # titre
plt.xlabel(r"f' (cm)")              # libellé des abscisses
plt.show()                          # afficher la figure
